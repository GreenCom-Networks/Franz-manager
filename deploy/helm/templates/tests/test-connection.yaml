apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "franz-manager.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "franz-manager.name" . }}
    helm.sh/chart: {{ include "franz-manager.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "franz-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
